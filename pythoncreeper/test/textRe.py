# coding:utf-8
import re
#创建模式对象
# pat=re.compile('AA')  #此处的AA是正则表达式。用来验证其他的字符串
# m=pat.search("CBAA")   #search字符串被校验的内容
# print(m)
#没有模式对象
# m=re.search("asd","Aasd")  #前面的字符串是规则（模板)，后面的字符串是被校验的对象
# print(m)

# print(re.findall("a","ASDaDFGAa"))  #前面的字符串是规则，后面的字符串是被校验的字符串
# print(re.findall("[A-Z]","ASDaDFGAa"))

#sub

# print(re.sub("a","A","abcdcasd")) #第一个字符串是被替换的 ，第二个是被替换的对象，

